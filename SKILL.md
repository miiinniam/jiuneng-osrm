# OSRM++ 项目总览（AI Agent 入口）

> **面向对象:** 任何能读 Markdown 的编码 Agent（Claude Code / Hermes / Codex / 人工）。
> **用途:** 这是整个 OSRM++ 项目的导航文档。读完本文档后，Agent 应能自行判断当前任务属于哪个子系统、应该打开哪个子 Skill 继续深入。

---

## 一句话架构

```
用户填表单 → 正算引擎(费用计算公式.py + cost_engine.py) → 报价输出
                                                    ↕ 共用同一套公式函数
供应商报价 → 反算引擎(calibration.py) → 拟合市场费率 → 更新车辆型号库.csv
```

**核心原则:** 正算和反算共用同一套公式函数（`费用计算公式.py`），确保"报价怎么算的，反推费率就用同一个口径"。

---

## 子系统导航

| 你要做什么 | 读哪个 Skill | 关键代码 |
|-----------|-------------|---------|
| 改运费公式（阈值/系数/公式结构） | [正算公式SKILL.md](公式反算文件/正算公式SKILL.md) | `费用计算公式.py` |
| 新增/修改/删除车型 | [车辆型号库SKILL.md](车辆型号库/车辆型号库变更指南SKILL.md) | `车辆型号库.csv` + `vehicle_registry.py` |
| 从供应商报价反推市场费率 | [运费反算SKILL.md](公式反算文件/运费反算SKILL.md) | `calibration.py` |
| 从原始报价文件提取数据 | [报价数据提取SKILL.md](公式反算文件/报价数据提取SKILL.md) | 数据提取脚本 |
| 部署/重启/油价更新 | `osrm-cost-calibration` (Hermes skill) | Docker Compose |

---

## 代码互联关系图

```
                    ┌──────────────────────┐
                    │  费用计算公式.py       │  ← 单一公式源
                    │  (所有计算逻辑)         │
                    └──────┬───────────────┘
                           │ import 公式
              ┌────────────┼────────────┐
              ▼            ▼            ▼
    ┌─────────────┐ ┌───────────┐ ┌──────────────┐
    │ cost_engine │ │calibration│ │ 反算样本验证   │
    │ (正算编排)   │ │ (反算拟合) │ │ (测试)        │
    └──────┬──────┘ └─────┬─────┘ └──────────────┘
           │              │
           │ 读 CSV       │ 读 CSV（同一份）
           ▼              ▼
    ┌──────────────────────────────┐
    │   车辆型号库.csv              │  ← 用户 Excel 维护
    │   vehicle_registry.py        │  ← 启动时一次性加载
    └──────────────────────────────┘
           │
           │ 读 presets
           ▼
    ┌──────────────┐    ┌──────────┐
    │  presets.py   │    │ config.py │
    │ (货物类型费率) │    │ (油价/工资)│
    └──────────────┘    └──────────┘
```

### 修改影响面速查

| 改了什么 | 必须同步改的地方 |
|---------|----------------|
| `费用计算公式.py` 的公式 | `cost_engine.py`（调用了该公式的地方）+ `calibration.py`（反算设计矩阵） |
| `费用计算公式.py` 的常量 | 不需要同步（只在这一个文件里） |
| `车辆型号库.csv` 的结构（加列） | `vehicle_registry.py` (VehicleModel dataclass + 解析) + `reference.py` (API 返回) + 前端 types.ts + i18n |
| `车辆型号库.csv` 的数值（改费率/载重） | 只需重启后端 |
| `presets.py` 的货物类型 | `cost_engine.py` + `calibration.py`（都会从 CARGO_TYPE_RATES 读，自动同步） |
| `config.py` 的默认油价/工资 | 无需同步（运行时参数） |

---

## 当前系统状态（2026-07-16）

### 车辆型号库
- 5 大类（small_box / flatbed / high_side / container / cold_chain）
- 31 款具体车型
- 最新优化：载荷基于越南法规校准、冷链重构为集装箱尺码、新增低平板/重型设备类型

### 公式状态
- 单一公式源：`费用计算公式.py`（速度惩罚/休息/装卸/容量占比/距离/时间/油耗/固定费）
- 正算和反算共用 `计算总用时()` 函数
- 整车模式有超载校验（`cargo_weight > max_load` 直接报错）
- 油耗附加 = 货物类型 penalty + 车型 penalty（累加，非取最大值）
- 拼货自动匹配：在所有装得下的车型里选总价最低的

### 已完成的公式优化（2026-07-03）
1. 提取 `计算总用时()` — 消灭 3 处重复代码
2. 整车超载校验 — 防止无意义报价
3. 合并两个 `_design_row` — 减少维护面
4. `combined_rate_multiplier` → `cargo_rate_multiplier` — 变量名去歧义
5. 油耗叠加注释 — 防止未来维护者误解叠加逻辑

### 公式版本管理规范

**`费用计算公式.py` 是唯一有独立版本的公式文件。** 每次改公式（改阈值/系数/新增函数/改函数签名），必须在文件头部的 `版本历史` 块加一条记录：

```
版本历史
========
vX.Y  YYYY-MM-DD  改了什么（一句话）
                  — 具体改动 1
                  — 具体改动 2
```

**编排层（`cost_engine.py`、`calibration.py`）不维护独立版本号**，只标注最后一次同步公式版本的日期。它们的改动在公式版本记录的"具体改动"中说明。

**`车辆型号库.csv` 不维护代码版本号**，但每次校准更新费率后，在对应车型的 `notes` 列标注更新日期和数据来源（如 `2026-07-03 校准更新，源:DuyAnh报价`）。

**当前公式版本: v1.2（2026-07-16）**

---

## 核心闭环工作流：反算优化 → 推导正算 → 应用上线

这是整个系统最重要的价值闭环。完整流程如下：

```
┌─────────────────────────────────────────────────────────────┐
│ 阶段 1: 反算校准（找出现有公式参数应该调成多少）              │
│                                                             │
│  供应商报价/历史运单                                          │
│       ↓ [报价数据提取SKILL.md]                                │
│  samples_*.json                                              │
│       ↓ [运费反算SKILL.md]                                    │
│  calibration.py → 拟合输出:                                   │
│    · base_rate_vnd_per_km (per model_id)  ← 这些要写回 CSV   │
│    · wage_hourly_vnd                      ← 这些要写回 config │
│    · fuel_price_vnd                       ← 这些要写回 config │
│    · rmse_vnd + warnings                   ← 判断能不能采用   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 阶段 2: 人工审核（判断哪些拟合值可信）                         │
│                                                             │
│  检查清单（见运费反算SKILL.md §5）:                            │
│  ☐ warnings 为空？                                           │
│  ☐ rmse_vnd 可接受？                                         │
│  ☐ 各车型 base_rate 不是负数？                                │
│  ☐ wage/fuel_price 不是负数？                                │
│  ☐ 条件数 < 1e6？                                            │
│                                                             │
│  如果有多家供应商的样本：跑多次校准，对比拟合值是否一致          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 阶段 3: 应用到正算公式（更新实际运行的参数）                    │
│                                                             │
│  3a. 更新车辆型号库.csv:                                      │
│      改 base_rate_vnd_per_km 列 → 写入校准输出的值             │
│      （只改数据可信的 model_id，不强行改样本不足的车型）          │
│                                                             │
│  3b. 更新 config.py:                                         │
│      default_wage_hourly_vnd → 校准输出（如果可信）            │
│      default_fuel_price_vnd → 校准输出或 Petrolimex 实时值     │
│                                                             │
│  3c. 重启后端: docker restart osrmplus-backend                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 阶段 4: 验证（确认更新后的正算公式输出合理）                    │
│                                                             │
│  1. 正算单测: pytest test_cost_engine.py                      │
│  2. 手算验算: 用一组已知输入跑正算，逐项检查数字量级              │
│  3. 拼货验证: 确认新费率下拼货自动匹配选车逻辑是否合理            │
│  4. 对比: 新费率报价 vs 原始供应商报价，误差是否在可接受范围       │
└─────────────────────────────────────────────────────────────┘
```

### 参数映射速查：校准输出 → 正算输入

| 校准输出 | 目标文件 | 目标字段 | 说明 |
|---------|---------|---------|------|
| `base_rate_vnd_per_km["flatbed_13m"]` | `车辆型号库.csv` | 该 model_id 行的 `base_rate_vnd_per_km` 列 | 每个车型独立更新 |
| `wage_hourly_vnd` | `backend/app/config.py` | `default_wage_hourly_vnd` | 全局参数 |
| `fuel_price_vnd` | `backend/app/config.py` | `default_fuel_price_vnd` | 全局参数；也可用 Petrolimex 实时值覆盖 |

### 安全护栏

- **不要盲信单次拟合结果。** 如果 warnings 里有负数费率/高条件数，先补充更多样化的样本再重跑（见运费反算SKILL.md §3）。
- **部分采纳。** 如果 6 款车型里有 4 款样本充足、2 款只有 1-2 条样本，只更新那 4 款，不要强行改样本不足的。
- **对比历史版本。** 更新 CSV 前备份一份（如 `车辆型号库_20260703.csv`），方便回滚对比。

---

## Agent 使用指南

### 收到"改运费公式"类任务时

1. 打开 [正算公式SKILL.md](公式反算文件/正算公式SKILL.md) 看公式架构
2. 改 `费用计算公式.py`（唯一公式源）
3. 同步改 `cost_engine.py`（编排层）和 `calibration.py`（反算设计矩阵）
4. 跑 `pytest backend/tests/` 验证

### 收到"改车型"类任务时

1. 打开 [车辆型号库SKILL.md](车辆型号库/车辆型号库变更指南SKILL.md) 看影响面清单
2. 确认改动类型（数值/结构/大类）
3. 按清单逐项改
4. 重启后端验证

### 收到"校准费率"类任务时

1. 打开 [运费反算SKILL.md](公式反算文件/运费反算SKILL.md) 看前置条件和运行方法
2. 确认样本数据在 `公式反算文件/供应商报价/` 下
3. 运行 `python -m app.services.calibration`
4. 解读拟合结果，人工判断是否应用到 CSV

---

## 文件索引

| 文件 | 角色 | 谁维护 |
|------|------|--------|
| `费用计算公式.py` | 公式定义（唯一源） | 开发者 |
| `cost_engine.py` | 正算编排 | 开发者 |
| `calibration.py` | 反算拟合 | 开发者 |
| `presets.py` | 货物类型费率 | 开发者 |
| `config.py` | 油价/工资默认值 | 开发者 |
| `vehicle_registry.py` | CSV 加载器 | 开发者 |
| `车辆型号库.csv` | 车型数据 | **用户（Excel 手动维护）** |
| `DEVELOPMENT_GOALS.md` | 项目设计文档 | 开发者（参考用，可能与代码不同步） |
