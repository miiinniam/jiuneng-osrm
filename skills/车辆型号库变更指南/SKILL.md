# 车辆型号库变更指南 —— 已迁移

最新版本请看：[`OSRM++/车辆型号库/车辆型号库变更指南SKILL.md`](../../车辆型号库/车辆型号库变更指南SKILL.md)

那边是实际工作目录（跟 `车辆型号库.csv` 同目录——车辆型号库是正算/反算共用的车辆主数据，
不属于反算专属，所以单独成目录，不在 `公式反算文件/` 里），这个文件只留一个指路，
避免两份文档内容不同步、越改越不一致。
