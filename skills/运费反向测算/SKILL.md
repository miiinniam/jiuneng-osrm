# 运费反向测算 —— 已迁移

最新版本请看：[`OSRM++/公式反算文件/运费反算SKILL.md`](../../公式反算文件/运费反算SKILL.md)

那边是实际工作目录（配套 `供应商报价/` 文件夹用来放各种供应商原始报价文件），
内容比这里更完整（新增了"如何从格式各异的原始报价单/费率表里提取样本数据"的
完整指南），这个文件只留一个指路，避免两份文档内容不同步、越改越不一致。
