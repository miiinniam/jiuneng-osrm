# OSRM++ 整车/拼车拆分 —— 完成度审查

**审查时间:** 2026-07-04  
**原始需求 vs 完成状态:**

## ✅ 已完成 (Phase 1-3)

| 需求 | 状态 | 文件 |
|------|:---:|------|
| 整车正算公式独立（距离×全包基价） | ✅ | `费用计算公式.py` + `cost_engine.py` |
| 拼车正算公式保持（三项拆分） | ✅ | 未改 `match_consolidated_model` |
| 整车反算只拟合 base_rate | ✅ | `calibration.py` |
| 前端模式选择器提到顶部 | ✅ | `QuoteForm.tsx` |
| 步骤随模式动态变化 | ✅ | 整车3步 / 拼车4步 |
| Docker Desktop 全栈运行 | ✅ | `docker-compose.yml` |
| i18n 新增 selectModel | ✅ | zh/en/vi.ts |

## 🔴 需立即修复 (高)

| # | 问题 | 文件 |
|---|------|------|
| 1 | **整车模式无法选货物类型** — cargoType 选择器只在拼车步骤中 | `QuoteForm.tsx` |
| 2 | **`buildRequest` 强制要求 weightKg>0** — 整车重量仅做超载校验 | `page.tsx` |
| 3 | **CostPanel 对整车显示 cost_time=0 / cost_fuel=0** — 应隐藏或标注 | `CostPanel.tsx` |

## 🟡 待完成 (Phase 4)

| 需求 | 说明 |
|------|------|
| 经停地址折叠+按钮 | 在AB之间用小 `+` 增加经停地 |
| 整车费率实际校准 | 用供应商报价数据跑一次反算 |
| 测试更新 | `test_cost_engine.py` / `test_calibration.py` |

---

## 修复计划 (bite-sized)

### Fix 1: 整车模式增加货物类型选择器
`QuoteForm.tsx` — 在 Vehicle step (step 1 for full_truck) 的车型选择器上方加 cargoType 下拉

### Fix 2: 整车模式放开 weightKg 必填
`page.tsx` line 88 — 整车模式下 skip weight validation

### Fix 3: CostPanel 区分整车/拼车展示
`CostPanel.tsx` — 传入 loadingMode，整车隐藏 time/fuel breakdown 行，改为"全包费率"标签

### Fix 4: 经停地址
暂缓，Phase 1-3 审查后再做
