# OSRM++ 运费算法深度分析报告

> **分析日期**: 2026-07-17  
> **分析范围**: 当前算法 vs 行业先进做法  

---

## 第一部分：当前算法完整画像

### 1.1 架构概览

当前系统采用 **经典成本加成（Cost-Plus）** 定价模型，分为两层：

| 层 | 文件 | 职责 |
|---|------|------|
| **公式层** | `费用计算公式.py` | 纯数学函数：速度惩罚、休息、油耗、距离/时间/附加费计算 |
| **编排层** | `cost_engine.py` | 工程逻辑：车型匹配、货物校验、结果组装、建议系统 |
| **校准层** | `calibration.py` | 最小二乘法反算：从供应商报价拟合基础费率 |

### 1.2 公式结构（拼货模式）

```
cost_total = [
    distance_km × base_rate × cargo_multiplier × (1 + return_factor)
  + total_duration_h × wage_hourly
  + distance_km × (fuel_L/100km ÷ 100) × fuel_price × load_factor × (1 + fuel_penalty)
  + toll + body_surcharge + road_surcharges + loading + insurance + misc
] × capacity_ratio  （仅 shareable 部分）

装卸费/保险费/杂费不打折 —— "本单专属"费用全额计
```

### 1.3 公式结构（整车模式）

```
cost_total = distance_km × base_rate × cargo_multiplier × (1 + return_factor)
           + toll + body_surcharge + road_surcharges + loading + insurance + misc

注：整车 base_rate 是"全包费率"——油耗/司机时间已隐含在基价中，不再拆分 cost_time / cost_fuel
```

### 1.4 子模型清单

| 子模型 | 类型 | 参数 | 问题 |
|--------|------|------|------|
| **速度惩罚** | 3档阶梯函数 | 阈值: 0.5, 0.8; 系数: 1.0, 1.15, 1.25 | 阶梯跳跃不连续(0.5→0.51突然跳15%) |
| **油耗载重修正** | 分段线性(deadband) | 起始0.5, 斜率0.2 | 线性假设过强，未区分车型 |
| **休息时间** | 规则累积 | 4h→0.5h, >8h→+8h | 硬编码规则，无法适应实际路况 |
| **装卸时间** | 线性 | 0.5h/吨 | 无车型差异(冷链装卸更慢) |
| **距离成本** | 完全线性 | base_rate × distance | 忽略长短途差异(长途应有规模效应) |
| **燃料附加** | 线性乘积 | fuel_penalty 货物+车辆叠加 | 叠加逻辑合理但缺乏实测校准 |
| **容量占比** | max(重量比, 体积比) | — | 未考虑"实际装载效率"(形状/堆叠) |
| **校准(OLS)** | 线性最小二乘 | 无正则化 | 小样本过拟合风险，无交叉验证 |

### 1.5 车型库数据特征

- 31个车型，5大类(small_box, flatbed, high_side, container, cold_chain)
- **22个车型 base_rate 仅靠估算**（标注"待实际报价核实"），9个有供应商数据校准
- 平板车/高栏车混合使用"纯公里费率"和"固定调度费+低公里费率"两种模型
- 无季节性调整、无区域差异化

---

## 第二部分：行业先进算法全景对比

### 2.1 算法演进路线图

```
第一代：经验报价（人工查表/Excel）  ← 大多数越南中小物流公司
   ↓
第二代：成本加成线性模型              ← OSRM++ 当前位置
   ↓
第三代：多因素非线性回归 + 正则化     
   ↓
第四代：集成学习（XGBoost / LightGBM）+ 特征工程
   ↓
第五代：深度学习 + 实时市场动态定价 + 强化学习
   ↓
第六代：端到端 AI（多模态：图像+文本+结构化+路线）
```

### 2.2 七种方案对比矩阵

#### 方案 A：梯度提升树回归（XGBoost / LightGBM）

| 维度 | OSRM++ 当前 | 梯度提升树方案 |
|------|------------|---------------|
| **模型类型** | 线性（解析式） | 非线性（树集成） |
| **特征交互** | 无（所有项独立相加） | 自动学习交互（距离×车型×重量×路线类型） |
| **非线性** | 几乎无（仅 3 档阶梯+燃油 deadband） | 全自动拟合任意非线性关系 |
| **正则化** | 无 | L1/L2 正则化，防过拟合 |
| **特征重要性** | 人工设计（固定系数） | 自动输出特征重要性排名 |
| **预测精度** | RMSE 依赖样本覆盖 | 通常比线性模型低 15-40% RMSE |
| **可解释性** | ⭐⭐⭐⭐⭐ 公式透明 | ⭐⭐⭐ SHAP 值可解释但不如解析式直观 |
| **样本需求** | 每车型 3-5 个即可标定 | 建议每车型 30+ 样本 |
| **冷启动** | 好（靠物理模型兜底） | 差（需要一定数据积累） |
| **工程复杂度** | 低（纯 Python 无依赖） | 中（需 sklearn/xgboost） |

**典型应用**: Flexport、Uber Freight 内部定价模型。

#### 方案 B：GLM with Regularization（弹性网络回归）

| 维度 | OSRM++ 当前 | 弹性网络方案 |
|------|------------|------------|
| **模型类型** | OLS（无正则化） | GLM + L1/L2 混合正则化 |
| **多重共线性处理** | 靠条件数检测+警告 | Elastic Net 自动处理 |
| **特征选择** | 人工选择 | L1 自动特征选择 |
| **预测区间** | 无 | 可输出置信区间 |
| **离群值鲁棒性** | 差 | Huber loss 提升鲁棒性 |

公式: `min β { ||y - Xβ||²₂ + λ₁||β||₁ + λ₂||β||²₂ }`

#### 方案 C：层次贝叶斯模型（Hierarchical Bayesian）

| 维度 | OSRM++ 当前 | 层次贝叶斯方案 |
|------|------------|---------------|
| **先验知识整合** | 隐式（基价硬编码） | 显式（先验分布编码领域知识） |
| **不确定性量化** | 无 | 完整后验分布，天然输出置信区间 |
| **小样本学习** | 物理模型兜底 | 层级共享信息（相似车型互相借力） |
| **多层级结构** | 全平铺（31车型独立） | 层级：区域→车型大类→具体型号 |
| **可扩展性** | 每加车型=多一个未知数 | 新车型自动继承同大类先验 |

#### 方案 D：深度神经网络 (DNN) / TabNet
- 特征交互：学习任意阶交互
- 表示学习：Embedding 层学习车型/区域/货物的稠密表示
- 多任务学习：共享底层 + 车型特定头
- 适用场景：样本量 1000+；风险：过拟合、黑箱

#### 方案 E：强化学习动态定价
- 定价策略：环境自适应（供需平衡）
- 目标：最大化长期收益（成交率 × 利润 - 空驶成本）
- State: (路线类型, 距离, 季节性, 竞争强度, 运力利用率)
- 适用场景：有大量报价/成交数据的平台型物流公司

#### 方案 F：活动成本法 (Activity-Based Costing, ABC)

```
运输总成本
├── 直接成本（燃油/路桥/工资/装卸）
├── 车辆成本（折旧/维护/轮胎/保险）
├── 管理费用（调度/仓储/单证）
└── 风险成本（货损/延误）
```

#### 方案 G：混合模型（Hybrid: Physics + ML）⭐ 推荐

```
price = cost_physical_formula + α × ML_residual_correction
α 权重随样本量动态调节（数据少时 α=0，纯物理；数据多了 α 逐渐增大到 0.8）
```

---

## 第三部分：值得修改和升级的具体方案

### 升级路线图

```
优先级       方案                  难度    风险    收益
─────────────────────────────────────────────────────
P0-立即    → 当前公式微调           低      低     中
P1-短期    → 校准方法升级            中      低     高
P2-中期    → 混合模型(Physics+ML)   中高    中     高
P3-长期    → 特征工程 + GBDT       中高    中     高
P4-愿景    → 动态定价 / 贝叶斯      高      中高   极高
```

---

### P0-立即：公式微调（零新依赖，当天可做）

#### P0.1 速度惩罚平滑化
**问题**: 阶梯函数在载重比 0.50→0.51 时突然跳 15%  
**方案**: 改为连续的分段线性

```python
# 改进（连续）:
def 速度系数(载重比例):
    if 载重比例 <= 0.3: return 1.00
    elif 载重比例 <= 0.8: return 1.00 + 0.30 * (载重比例 - 0.3) / 0.5
    else: return 1.30 + 0.10 * (载重比例 - 0.8) / 0.2  # 1.3→1.4
```

#### P0.2 油耗修正系数引入车型差异
**问题**: 所有车型用相同斜率 0.2，但重卡满载油耗增幅远超轻卡  
**方案**: 车型 CSV 新增 `fuel_load_slope` 列（轻卡 0.15, 重卡 0.25, 平板 0.18）

#### P0.3 长途规模效应（距离衰减因子）
**问题**: 纯线性——500km 成本是 50km 的 10 倍，但实际长途均价更低  
**方案**:
```python
def 距离衰减因子(距离公里):
    if 距离公里 <= 100: return 1.0
    elif 距离公里 <= 500: return 1.0 - 0.1 * (距离公里 - 100) / 400
    elif 距离公里 <= 1500: return 0.90 - 0.05 * (距离公里 - 500) / 1000
    else: return 0.85
```

#### P0.4 装卸费车型化
**问题**: 所有车型同等 50,000 VND/吨，冷链/重型设备装卸差异大  
**方案**: 车型 CSV 新增 `loading_rate_vnd_per_ton`（可选，默认继承全局值）

---

### P1-短期：校准方法升级

#### P1.1 加权最小二乘（Weighted OLS）
样本权重 = exp(-λ × 样本年龄天数)，新报价权重 > 旧报价

#### P1.2 交叉验证 + 留出集
k-fold CV，输出训练/验证 RMSE 和各折费率稳定性

#### P1.3 异常值检测
RANSAC 或 Huber 回归，自动排除录入错误/特殊折扣

#### P1.4 样本质量评分
增加 `sample_quality` 字段 (1-5)，低分样本在拟合中降权

---

### P2-中期：混合模型 Physics + ML ⭐ 推荐

架构设计：

```
                   输入特征
                      │
    ┌─────────────────┼─────────────────┐
    │                 │                 │
    ▼                 │                 ▼
物理公式引擎          │           ML 残差预测器
price_phys = Σ各子项  │           residual = XGBoost(X)
(确定性,可解释)       │           (学习非线性残差)
    │                 │                 │
    └────────┬────────┘                 │
             │                          │
             ▼                          ▼
    price = price_phys + α × residual
    α: 样本<30 → 0.0 | 30-100 → 递增 | >100 → 0.8
```

**优势**：物理公式保底 + ML 补非线性 + 渐进过渡 + 可回退

#### 特征工程

从现有数据衍生（无需外部数据源）:
- 基础: distance_km, weight_ton, volume_m3, vehicle_base_rate, ...
- 衍生: log_distance, weight_ratio, volume_ratio, load_density, is_long_haul, is_heavy
- 比值: rate_to_fuel_ratio, weight_to_volume_ratio
- One-hot: cargo_normal, cargo_cold_chain, ...; cat_small_box, cat_flatbed, ...

#### 模型参数
```python
xgb.XGBRegressor(
    n_estimators=100, max_depth=4, learning_rate=0.05,
    subsample=0.8, reg_lambda=1.0, reg_alpha=0.5,
)
```

---

### P3-长期：纯 GBDT 端到端

| 对比 | 当前 OLS | 混合模型 | 纯 GBDT |
|------|---------|---------|--------|
| **推荐样本量** | 10+ | 30+ | 200+ |
| **RMSE 改进** | baseline | -10~20% | -20~40% |
| **可解释性** | 极好 | 好 | 中(SHAP) |
| **维护成本** | 极低 | 中 | 中高 |

自动发现的交互模式: 短途/长途分段、车型×距离×重量三维交互、区域效应、油价弹性

---

### P4-愿景：动态定价 + 市场感知

#### P4.1 赢单率模型
记录每条报价的成交结果 → 建模 `P(win | price, route, cargo)` → 最优报价 = argmax(赢单率 × 利润)

#### P4.2 区域性基价
越南北中南三地成本结构不同，添加 origin/dest region 标签

#### P4.3 季节性/周期性因子
农历年前 +15~25%、雨季山区 +10~20%、节假日 +5~10%

---

## 第四部分：执行建议

### 6个月路线图

```
第 1-2 周:  P0 公式微调（速度平滑+距离衰减+车型油耗斜率）
第 3-4 周:  P1 校准升级（时间加权OLS+CV+RANSAC）
第 5-8 周:  P2 混合模型原型（XGBoost残差+特征工程+α渐进策略）
第 9-16 周: P2 迭代优化（积累样本+区域化特征+SHAP解释前端）
第 17-24 周: P3/P4 探索（纯GBDT端到端/赢单率模型）
```

### 数据积累策略

| 优先级 | 数据项 | 来源 | 用途 |
|--------|--------|------|------|
| 🔴 关键 | 实际成交价格 | 用户手动回填 | 校准目标、赢单率 |
| 🔴 关键 | 区域性标签 | 路线输入自动标注 | 区域基价 |
| 🟡 重要 | 报价-成交时间差 | 系统自动记录 | 市场热度 |
| 🟡 重要 | 季节性标签 | 系统自动（日期） | 周期因子 |
| 🟢 增益 | 竞争对手价格 | 用户手动输入 | 市场定位 |

### 风险提示

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| ML 过拟合（样本不足） | 🔴 高 | α 权重 + 物理公式兜底 |
| 报价不反映市场价 | 🟡 中 | 增加"实际成交价"字段 |
| 用户不接受黑箱 | 🟢 低 | 混合模型保留物理公式 + SHAP |
| 过度工程化 | 🟢 低 | P0→P1 先跑，评估收益 |

---

## 第五部分：总结

### 当前算法定位
OSRM++ 是一个**设计精良的第二代成本加成线性模型**：架构清晰、物理约束、双模式、可审计。

### 核心不足
- ❌ 完全线性（无长短途差异、无区域差异、无密度效应）
- ❌ 无交互（所有成本项独立相加）
- ❌ 无不确定性（点估计无置信区间）
- ❌ 无市场感知（成本加成而非市场导向）
- ❌ 校准方法简单（OLS无正则化无CV无时间衰减）
- ❌ 22/31 车型依赖估算而非数据

### 推荐路径
**P2 混合模型（Physics + ML）** 是投资回报比最高的方案，用 XGBoost 学习物理公式残差，渐进式过渡，永远可回退。

### 立竿见影的改进
1. 速度惩罚连续化 — 消除阶梯跳跃
2. 距离衰减因子 — 解决长途不降价
3. 时间加权 OLS — 新报价更有发言权
4. 异常值检测 — 自动排除录入错误

---

## 第六部分：子调研补充（学术+工业验证）

> 以下内容来自独立子调研 agent，通过 Firecrawl 搜索 10+ 学术与工业来源。

### 6.1 学术验证

| 研究 | 算法 | 数据集 | 结论 |
|------|------|--------|------|
| **Jang et al. (2023)** MDPI *Sustainability* | XGBoost vs LightGBM vs DNN vs 线性回归 | 货运经纪平台运单 | **XGBoost / LightGBM 并列最优** |
| **Valatsos et al. (2021)** ACM PCI 2021 | Random Forest vs NGBoost vs XGBoost vs Bagging | 货运路线时间预测 | **RF / NGBoost 最优**；RF 在小数据集上泛化极佳 |
| **Omidkar et al. (2025)** *Cleaner Logistics and Supply Chain* | RF vs 多元线性回归 vs ANN | 生物质运输成本（15个独立变量） | **RF R²=97.4%，RMSE=165**；最关键因素：载重 37%、车型 31% |

### 6.2 工业落地案例

| 公司/平台 | 方法 | 效果 |
|-----------|------|------|
| **Ontruck**（欧洲数字货运平台） | XGBoost 现货定价 | 实时响应供需，自动识别定价机会和运营效率洼地 |
| **project44** | 实时运价引擎 + 线路情报 | 全球最大供应链可视化平台 |
| **AWS LSP 框架**（2023） | ML三阶段：增强分析→行为模型→市场视图 | **预测精度+13%，核心承运人+15%，服务线路+10%** |
| **DAT Freight & Analytics / SONAR (FreightWaves)** | Lane级市场基准（Load-to-Truck Ratio） | 北美卡车货运行业标准指数 |

### 6.3 动态定价七策略（Nexocode 分类）

1. **Cost-Plus（成本加成）** — 成本 + 固定利润率 ← *OSRM++ 当前*
2. **Competitor-Based（竞对参考）** — 参考市场竞对报价
3. **Value-Based（价值定价）** — 按客户感知价值（紧急程度、服务水平）
4. **Time-Based（时间定价）** — 淡旺季、节假日、高峰时段
5. **Price Discrimination（价格歧视）** — 按客户类型（大客户 vs 散客）差异化
6. **KVI-Based（高频线路锚定）** — 高频线路定基准价，带动其他线路
7. **Predatory Pricing（掠夺性定价）** — 战略性低价抢占市场（慎用）

推荐AI算法：贝叶斯模型、**强化学习**、Thompson Sampling、决策树

### 6.4 市场基准核心指标

```
Load-to-Truck Ratio (LTR) = 线路待运货物数 / 可用车辆数

LTR > 5  → 市场"热"（需求 >> 供给）→ 加价
LTR < 2  → 市场"冷"（供给 >> 需求）→ 降价
2 ≤ LTR ≤ 5 → 市场平衡 → 参考历史中位价
```

⚠️ **越南市场现状**: 暂无 DAT/SONAR/Xeneta 级别的公开货运价格指数。中越跨境线路高度固定（友谊关、老街、芒街等口岸），可针对性自建内部线路价格数据库。

### 6.5 学界推荐的最佳实践

综合三篇学术论文和两个工业框架，对 OSRM++ 的最优路径是：

```
Phase 1 (1-2月): XGBoost + ABC/TCO成本细化 + 自建内部线路价格基准
Phase 2 (3-6月): Random Forest集成 + 半动态定价 + 特征工程增强  
Phase 3 (6-12月): 外部数据集成 + DNN探索 (仅数据量>10000条后)
```

**核心原则**: XGBoost 是货运成本预测的学术+工业双重验证最优选择；OSRM++ 的组件分解结构应保留作为 ML 模型的**特征输入**，两者互补而非替代。
